# Kubernetes Secret 예시 파일
# 실제 사용 시 secret.yml로 복사하여 실제 값으로 변경하세요
# cp secret.example.yml secret.yml

#apiVersion: v1
#kind: Secret
#metadata:
#  name: board-secret
#  namespace: board-app
#type: Opaque
#stringData:
#  # PostgreSQL 설정 (Helm으로 배포된 PostgreSQL 연결)
#  DB_URL: "jdbc:postgresql://postgres-postgresql.postgres:5432/board"
#  DB_USERNAME: "your-username"
#  DB_PASSWORD: "your-password"
#  DB_POOL_SIZE: "10"

  # JWT Secret (향후 사용)
  # JWT_SECRET: "your-jwt-secret-key-min-256-bits"

  # 파일 업로드 경로
  # FILE_UPLOAD_DIR: "/app/uploads"
