apiVersion: apps/v1
kind: Deployment
metadata:
  name: board-backend
  namespace: board-app
  labels:
    app: board-backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: board-backend
  template:
    metadata:
      labels:
        app: board-backend
    spec:
      containers:
        - name: board-backend
          image: suehunpark/board-be:6f4daa6c835e3fb782c3e14dc1eb8b235ef15c64
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http

          volumeMounts:
            - name: uploads-storage
              mountPath: /app/uploads

          env:
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: board-secret
                  key: DB_URL
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: board-secret
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: board-secret
                  key: DB_PASSWORD
            - name: DB_POOL_SIZE
              valueFrom:
                secretKeyRef:
                  name: board-secret
                  key: DB_POOL_SIZE
            - name: SPRING_PROFILES_ACTIVE
              value: dev

          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"

          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 5

      volumes:
        - name: uploads-storage
          persistentVolumeClaim:
            claimName: board-uploads-pvc
